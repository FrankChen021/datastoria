# ============================================================================
# ClickHouse Console Environment Variables
# ============================================================================
# Copy this file to .env and configure your settings
# cp .env.example .env

# ============================================================================
# AUTHENTICATION CONFIGURATION (Optional)
# ============================================================================
# Authentication is optional. If no providers are configured, the app will
# run without authentication.

# ----------------------------------------------------------------------------
# NextAuth Secret (Required if authentication is enabled)
# ----------------------------------------------------------------------------
# Generate a random secret with: openssl rand -base64 32
# This secret is used for JWT encryption and session management
NEXTAUTH_SECRET=your-secret-key-here-replace-with-random-32-char-string

# ----------------------------------------------------------------------------
# Google OAuth Provider
# ----------------------------------------------------------------------------
# Get credentials from: https://console.cloud.google.com/apis/credentials
# 1. Create a new OAuth 2.0 Client ID
# 2. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google
# 3. Copy the Client ID and Client Secret

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Set to "true" to show Google login button on the login page
NEXT_PUBLIC_GOOGLE_ENABLED=true

# ----------------------------------------------------------------------------
# GitHub OAuth Provider
# ----------------------------------------------------------------------------
# Get credentials from: https://github.com/settings/developers
# 1. Click "New OAuth App"
# 2. Set Authorization callback URL: http://localhost:3000/api/auth/callback/github
# 3. Copy the Client ID and generate a Client Secret

GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Set to "true" to show GitHub login button on the login page
NEXT_PUBLIC_GITHUB_ENABLED=true

# ----------------------------------------------------------------------------
# Microsoft Entra ID (Azure AD) OAuth Provider
# ----------------------------------------------------------------------------
# Get credentials from: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps
# 1. Register a new application
# 2. Add redirect URI: http://localhost:3000/api/auth/callback/microsoft-entra-id
# 3. Create a client secret
# 4. Copy the Application (client) ID, Directory (tenant) ID, and client secret

MICROSOFT_CLIENT_ID=your-microsoft-client-id
MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret
MICROSOFT_TENANT_ID=your-microsoft-tenant-id

# Set to "true" to show Microsoft login button on the login page
NEXT_PUBLIC_MICROSOFT_ENABLED=true

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

# ----------------------------------------------------------------------------
# Next.js Configuration
# ----------------------------------------------------------------------------
# Uncomment and configure if deploying to a subdirectory
# NEXT_PUBLIC_BASE_PATH=/clickhouse-console

# Production URL (for OAuth callbacks in production)
# NEXTAUTH_URL=https://your-domain.com

# ============================================================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================================================
# When deploying to production:
# 1. Generate a strong NEXTAUTH_SECRET
# 2. Update OAuth redirect URIs to use your production domain
# 3. Set NEXTAUTH_URL to your production URL
# 4. Use HTTPS in production (required for OAuth)
# 5. Never commit .env file to version control
#
# Example production OAuth callback URLs:
# - Google:    https://your-domain.com/api/auth/callback/google
# - GitHub:    https://your-domain.com/api/auth/callback/github
# - Microsoft: https://your-domain.com/api/auth/callback/microsoft-entra-id

# ============================================================================
# DOCKER DEPLOYMENT
# ============================================================================
# When using Docker, pass environment variables via:
# 1. Docker run: docker run -e NEXTAUTH_SECRET=xxx -e GOOGLE_CLIENT_ID=xxx ...
# 2. Docker Compose: Configure in docker-compose.yml environment section
# 3. .env file: Mount as volume: -v $(pwd)/.env:/app/.env
